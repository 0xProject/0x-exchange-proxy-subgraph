{
  "name": "0x-exchange-proxy-subgraph",
  "version": "0.1.0",
  "scripts": {
    "create": "graph create dekz/0x-exchange-proxy --node https://api.thegraph.com/deploy/",
    "create-local": "graph create dekz/0x-exchange-proxy --node http://127.0.0.1:8020",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy dekz/0x-exchange-proxy --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy dekz/0x-exchange-proxy --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "prettier": "prettier src/**/*.ts src/*.ts --config ./.prettierrc"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
